# 基础类

设计了人物类，设计了动作类，设计了tile类，region类，设计了时间类，模拟器类，前端类，灵气类，修仙等级类，灵根类，寿命类，事件类，NPC AI类，角色性格类，id类，动物类，Item类，灵石类，植物类。

在动作进行时可以异步实现，实行协程化机制

## 人物类：包含性别，姓名，出生年月等，描述人物的一切信息，与动作类相绑定，可以获取修仙进度，检查是否老死，获取年龄信息，获取历史动作。

## 动作类：角色可以执行的动作，添加判断动作是否可执行的方法，分为实际动作和情绪动作。需要包含移动动作，修炼动作，突破动作，娱乐动作，狩猎动作，采集动作。

## tile类：包含最基础的一个方格的tile类，由region组成的map类。

## region类：由tile组成，但是需要在后续添加不同的region子类，城市，修炼区域，普通区域，所以独立出来，需要shape类来描述形状

## 时间类：由年类，月类组成，共同组成时间戳类。

## 模拟器类：用于结算人物的事件（需要设计如何进行结算，因为游戏是以方格为基础的，所以以前进一步进行一次结算，每步模拟是一个月时间）,同时也要负责新角色的产生的老角色的死亡，还要记录世界的事件和世界的时间以及世界的变化。

## 前端类：用于验证后端逻辑和图形化游戏。

## 灵气类：描述某个地区的灵气状况。

## 修仙等级类：用于描述人物的修仙等级，验证是否能晋级，晋级的类。

## 灵根类：描述灵根类别。

## 寿命类：用于管理角色的死亡和剩余预期寿命。

## 事件类：记录世界上的事件的类。

## NPC AI类：给NPC做决策，一种是规则类AI，一种是LLM类AI。规则类AI动作固定，LLM类AI的动作应该分为两类，一种是动作链，一种是突发状况(NPC/世界发起的)

## 角色性格类：用于描述角色的性格。

## id类：用于生成人物的id。

## 动物类：用于描述动物。

## Item类：用于描述角色拥有的物品。

## 灵石类：用于描述角色拥有的灵石数量（归到物品类中更好？）

## 植物类：描述世界上的植物状况。


# 额外文件

需要添加名称文件，图片文件，LLM文件，配置文件，IO文件

## 名称文件：用于随机生成人物姓名

## 图片文件：用于在地图上显示人物图像

## LLM文件：用于调用LLM的文件，需要提示词模板

## 配置文件：用于边界导入yml数据的文件

## IO文件：用于读入txt文件